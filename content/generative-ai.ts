import type { Lesson, Section } from "@/lib/content";

type Spec = { slug: string; title: string; subtitle: string; minutes: number; tags: string[]; description: string; chart: string; install?: string; concept: string; code: string; gotcha: string; question: string; options: string[]; correct: number; explanation: string; next: string };

const specs: Spec[] = [
  { slug: "generative-models-setup", title: "Generative Models: Setup and Big Picture", subtitle: "Install the tools and distinguish generating data from predicting labels.", minutes: 18, tags: ["Installation", "Generative models", "Latent space"], description: "Discriminative models learn a boundary or label from input; generative models learn enough structure about data to sample new, plausible examples such as text, images, or audio.", install: "python -m venv .venv\n# Windows PowerShell:\n.\\.venv\\Scripts\\Activate.ps1\npython -m pip install --upgrade pip\npython -m pip install torch torchvision transformers diffusers accelerate pillow", chart: "flowchart LR\n D[Training data] --> M[Generative model]\n M --> Z[Latent noise / prompt]\n Z --> S[New sample]\n M --> P[Estimate data patterns]", concept: "Many generative systems start from a simple latent variable—noise or a compact code—and learn to turn it into a sample following the training distribution.", code: "import torch\n# A latent vector is a compact source of variation for a generator.\nz = torch.randn(4, 128)\nprint(z.shape, z.mean().item(), z.std().item())", gotcha: "Generating realistic-looking output is not proof it is factual, unbiased, licensed, or safe. Treat generated content as an output requiring task-appropriate review.", question: "What is the main goal of a generative model?", options: ["Learn patterns in data well enough to create plausible new samples.", "Only assign a fixed label to each input.", "Remove all randomness from data.", "Guarantee factual outputs."], correct: 0, explanation: "Generative models model or approximate the data distribution, allowing sampling of new examples.", next: "Autoencoders introduce the key idea of compressing and reconstructing data." },
  { slug: "autoencoders", title: "Autoencoders", subtitle: "Compress inputs into a latent code and reconstruct them.", minutes: 24, tags: ["Autoencoder", "Encoder", "Decoder", "Latent space"], description: "An autoencoder learns an encoder that compresses an input and a decoder that reconstructs it. The bottleneck forces the representation to retain useful structure.", chart: "flowchart LR\n X[Input image] --> E[Encoder]\n E --> Z[Latent code]\n Z --> D[Decoder]\n D --> R[Reconstruction]\n X --> L[Reconstruction loss]\n R --> L", concept: "The model minimizes the difference between input and reconstruction. A compact latent space can support denoising, anomaly detection, compression, and feature learning.", code: "import torch.nn as nn\n\nautoencoder = nn.Sequential(\n    nn.Flatten(), nn.Linear(28*28, 64), nn.ReLU(),\n    nn.Linear(64, 28*28), nn.Sigmoid(),\n    nn.Unflatten(1, (1, 28, 28)),\n)\n# Train with MSE or BCE reconstruction loss against the input image.", gotcha: "A high-capacity autoencoder can learn near-identity copying rather than useful compression. Limit latent size or add regularization when representation quality matters.", question: "What is an autoencoder's training target?", options: ["Its own input, reconstructed after passing through a bottleneck.", "A random class label.", "Only the latent vector.", "A future token."], correct: 0, explanation: "The input acts as the target; the model learns to reconstruct it from a constrained code.", next: "VAEs make the latent space probabilistic and easier to sample from." },
  { slug: "variational-autoencoders", title: "Variational Autoencoders", subtitle: "Learn a smooth, sampleable probabilistic latent space.", minutes: 28, tags: ["VAE", "KL divergence", "Reparameterization"], description: "A VAE encodes an input as a distribution rather than one fixed code. Regularizing that distribution toward a standard normal makes it possible to sample coherent latent points.", chart: "flowchart LR\n X[Input] --> E[Encoder]\n E --> MU[Mean mu]\n E --> SD[Log variance]\n MU --> Z[Sample latent z]\n SD --> Z\n Z --> D[Decoder]\n D --> R[Reconstruction]", concept: "The VAE loss combines reconstruction quality with a KL-divergence term that keeps each encoded distribution close to a simple prior such as N(0, I). Reparameterization makes stochastic sampling differentiable.", code: "import torch\ndef reparameterize(mu, logvar):\n    std = torch.exp(0.5 * logvar)\n    eps = torch.randn_like(std)\n    return mu + eps * std\n\nmu, logvar = torch.zeros(4, 16), torch.zeros(4, 16)\nz = reparameterize(mu, logvar)\nprint(z.shape)", gotcha: "Do not sample with a non-differentiable operation that blocks gradients to the encoder. The reparameterization trick moves randomness into `eps` and preserves a differentiable path.", question: "Why does a VAE use the reparameterization trick?", options: ["It allows gradients to flow through stochastic latent sampling.", "It removes the decoder.", "It guarantees sharp images.", "It turns labels into tokens."], correct: 0, explanation: "Writing z as mu + sigma times random noise separates trainable quantities from the random draw.", next: "GANs take a different approach: adversarial training between generator and discriminator." },
  { slug: "gans", title: "Generative Adversarial Networks", subtitle: "Train a generator by competing against a discriminator.", minutes: 28, tags: ["GAN", "Generator", "Discriminator", "Adversarial"], description: "A GAN trains two networks together. The generator turns noise into samples; the discriminator tries to distinguish real training data from generated fakes.", chart: "flowchart LR\n Z[Random noise] --> G[Generator]\n G --> F[Fake sample]\n F --> D[Discriminator]\n R[Real sample] --> D\n D --> L[Adversarial losses]\n L --> G\n L --> D", concept: "The generator improves when it fools the discriminator; the discriminator improves when it detects fakes. This minimax game can produce sharp samples but is notoriously difficult to stabilize.", code: "import torch.nn as nn\nG = nn.Sequential(nn.Linear(64, 256), nn.ReLU(), nn.Linear(256, 784), nn.Tanh())\nD = nn.Sequential(nn.Linear(784, 256), nn.LeakyReLU(0.2), nn.Linear(256, 1))\n# Alternate: update D on real/fake batches, then update G to make D(fake) look real.", gotcha: "If the discriminator becomes far stronger than the generator, the generator may receive poor gradients. Monitor both losses and generated samples; do not interpret one loss in isolation.", question: "What is the discriminator's job in a GAN?", options: ["Distinguish real training examples from generated examples.", "Decode latent vectors into images.", "Tokenize text.", "Add Gaussian noise at inference."], correct: 0, explanation: "The discriminator supplies the adversarial learning signal that pushes the generator toward realistic samples.", next: "Diffusion models generate by learning to reverse a gradual noising process." },
  { slug: "diffusion-models", title: "Diffusion Models", subtitle: "Generate samples by denoising random noise step by step.", minutes: 30, tags: ["Diffusion", "Denoising", "Noise schedule"], description: "Diffusion training gradually corrupts real data with noise, then teaches a neural network to predict or remove that noise. Sampling starts from random noise and reverses the process.", chart: "flowchart LR\n X[Real image] --> N1[Add noise step 1]\n N1 --> NN[... add noise]\n NN --> Z[Pure noise]\n Z --> D[Learned denoise steps]\n D --> S[Generated image]", concept: "The forward process is fixed noising. The learned reverse model conditions on a noisy sample and timestep, gradually recovering data structure. Text prompts can guide this denoising through conditioning.", code: "import torch\nx0 = torch.randn(2, 3, 32, 32)\nnoise = torch.randn_like(x0)\nalpha_bar = 0.7\nxt = alpha_bar**0.5 * x0 + (1 - alpha_bar)**0.5 * noise\n# A denoiser learns to predict noise from xt and timestep t.\nprint(xt.shape)", gotcha: "Sampling quality depends on the scheduler, number of denoising steps, guidance, and model. Do not compare models using different scheduler settings without documenting them.", question: "What does a diffusion denoiser commonly learn to predict during training?", options: ["The noise added to a clean training sample at a given timestep.", "Only the original filename.", "A fixed class for every image.", "The next word in a sentence."], correct: 0, explanation: "Predicting the injected noise provides a convenient learning target for reversing the corruption process.", next: "Apply autoregressive generation to text, where samples are produced token by token." },
  { slug: "text-generation", title: "Text Generation", subtitle: "Generate coherent text from next-token probabilities.", minutes: 24, tags: ["Language generation", "Sampling", "Temperature"], description: "Autoregressive language models predict a distribution for the next token given a prefix, append a selected token, and repeat until a stopping condition.", chart: "flowchart LR\n P[Prompt / prefix] --> M[Language model]\n M --> L[Next-token logits]\n L --> S[Temperature + sampling]\n S --> A[Append token]\n A --> P", concept: "Greedy decoding chooses the highest-probability token. Temperature changes distribution sharpness; top-k and top-p sampling restrict the candidate set to balance diversity and coherence.", code: "import torch\nlogits = torch.tensor([1.0, 2.0, 0.2, -1.0])\ntemperature = 0.8\nprobs = (logits / temperature).softmax(dim=-1)\nnext_token = torch.multinomial(probs, 1)\nprint(probs, next_token.item())", gotcha: "Do not equate a fluent continuation with a verified claim. Sampling controls style and variety, not factuality or grounding.", question: "What does lowering temperature usually do?", options: ["Makes the next-token distribution sharper and output more deterministic.", "Adds more context tokens.", "Trains the model faster.", "Guarantees correct facts."], correct: 0, explanation: "Dividing logits by a smaller temperature amplifies differences, concentrating probability on high-scoring tokens.", next: "Use diffusion pipelines to generate images from text conditions." },
  { slug: "image-generation-pipelines", title: "Image Generation Pipelines", subtitle: "Run a text-to-image diffusion pipeline responsibly.", minutes: 24, tags: ["Text-to-image", "Diffusers", "Prompts"], description: "Text-to-image systems combine a text encoder, latent denoising model, scheduler, and image decoder. They generate from a prompt while balancing speed, memory, and visual fidelity.", chart: "flowchart LR\n P[Text prompt] --> E[Text encoder]\n E --> C[Conditioning]\n Z[Latent noise] --> U[U-Net denoiser]\n C --> U\n U --> V[Latent decoder]\n V --> I[Image]", concept: "Latent diffusion performs denoising in a compressed latent space instead of raw pixels, which greatly reduces computation. Classifier-free guidance strengthens alignment with the text condition.", code: "# Requires the diffusers package and a compatible downloaded model.\nfrom diffusers import AutoPipelineForText2Image\nimport torch\n\npipe = AutoPipelineForText2Image.from_pretrained(\n    \"stabilityai/sdxl-turbo\", torch_dtype=torch.float16\n).to(\"cuda\")\nimage = pipe(prompt=\"a small watercolor lighthouse at sunrise\", num_inference_steps=4).images[0]\nimage.save(\"lighthouse.png\")", gotcha: "The example requires a compatible GPU and model access. On CPU or limited VRAM, choose a smaller model or a hosted inference service rather than forcing an out-of-memory run.", question: "Why do latent-diffusion models generate in a latent space?", options: ["It reduces denoising computation compared with operating on full-resolution pixels.", "It removes the text encoder.", "It guarantees no harmful images.", "It avoids every scheduler."], correct: 0, explanation: "A compact learned representation preserves visual structure while making repeated denoising significantly cheaper.", next: "Finish by combining text, image, and structured outputs in a multimodal workflow." },
  { slug: "multimodal-generation-capstone", title: "Multimodal Generation Capstone", subtitle: "Design a safe, testable workflow that combines text and images.", minutes: 30, tags: ["Multimodal", "Capstone", "Evaluation"], description: "Real generative products compose models into workflows: a user prompt is validated, text may be planned or rewritten, an image model generates assets, and outputs are checked and stored with metadata.", chart: "flowchart LR\n U[User request] --> V[Validate / moderate]\n V --> T[Text planning]\n T --> I[Image or audio generator]\n I --> Q[Quality / safety checks]\n Q --> O[Final multimodal response]", concept: "A production workflow needs more than generation: clear user intent, input/output controls, reproducible settings, evaluation examples, and a human escalation path for high-stakes content.", code: "request = {\n  \"prompt\": \"Illustrate a solar-powered village\",\n  \"style\": \"watercolor\",\n  \"seed\": 42,\n}\n# Record model ID, scheduler, seed, prompt, and safety outcome with each asset.\nmetadata = {\"model\": \"chosen-model\", \"seed\": request[\"seed\"], \"status\": \"reviewed\"}\nprint(metadata)", gotcha: "Do not hard-code one impressive prompt as your only evaluation. Build a varied prompt set, test failure modes, and record versions/settings so regressions can be diagnosed.", question: "What should be recorded with a generated asset for reproducibility?", options: ["Model/version, prompt, seed, scheduler/settings, and relevant review outcome.", "Only the final PNG filename.", "The user's browser color scheme.", "No metadata, to save space."], correct: 0, explanation: "Generation is stochastic and model-dependent; metadata makes outputs traceable, repeatable, and debuggable.", next: "You now understand major generative families and can choose among autoencoders, VAEs, GANs, diffusion, and autoregressive models for a concrete task." },
];

function sections(s: Spec): Section[] { const offset = s.install ? 1 : 0; return [
  { step: 1, title: "What it means", blocks: [{ type: "text", content: s.description }] },
  ...(s.install ? [{ step: 2, title: "Install it", blocks: [{ type: "code" as const, language: "bash", label: "Windows PowerShell", code: s.install }, { type: "callout" as const, kind: "tip" as const, content: "Use a virtual environment. For GPU generation, install the PyTorch build matched to your hardware and check VRAM requirements before downloading a model." }] }] : []),
  { step: 2 + offset, title: "Visual explanation", blocks: [{ type: "diagram", chart: s.chart, label: `${s.title} in context` }] },
  { step: 3 + offset, title: "Core concept", blocks: [{ type: "callout", kind: "insight", content: s.concept }] },
  { step: 4 + offset, title: "Build it", blocks: [{ type: "code", language: "python", label: "Minimal example", code: s.code }] },
  { step: 5 + offset, title: "Experiment", blocks: [{ type: "text", content: "Start on a tiny dataset or a single small prompt. Change one setting at a time, save representative outputs, and inspect both quality and failure cases." }] },
  { step: 6 + offset, title: "Common mistake", blocks: [{ type: "callout", kind: "gotcha", content: s.gotcha }] },
  { step: 7 + offset, title: "Test yourself", blocks: [{ type: "quiz", question: s.question, options: s.options, correct: s.correct, explanation: s.explanation }] },
  { step: 8 + offset, title: "What is next", blocks: [{ type: "text", content: s.next }] },
]; }

export const generativeAiLessons: Lesson[] = specs.map((s, index) => ({ slug: s.slug, trackSlug: "generative-ai", order: index + 1, minutes: s.minutes, title: s.title, subtitle: s.subtitle, tags: s.tags, sections: sections(s) }));
