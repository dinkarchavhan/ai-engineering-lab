import type { Lesson, Section } from "@/lib/content";

type Spec = { slug: string; title: string; subtitle: string; minutes: number; tags: string[]; description: string; chart: string; install?: string; concept: string; code: string; gotcha: string; question: string; options: string[]; correct: number; explanation: string; next: string };

const specs: Spec[] = [
  { slug: "tiny-gpt-setup-data", title: "Tiny GPT Setup and Training Data", subtitle: "Install the tools and turn a small text corpus into next-token examples.", minutes: 20, tags: ["Installation", "Corpus", "Language modeling"], description: "A language model learns one deceptively simple task: predict the next token from a prefix. A Tiny GPT makes every part of that task inspectable on a small text corpus.", install: "python -m venv .venv\n# Windows PowerShell:\n.\\.venv\\Scripts\\Activate.ps1\npython -m pip install --upgrade pip\npython -m pip install torch tiktoken\n\n# Optional: use a CUDA-compatible PyTorch build from pytorch.org for GPU training.", chart: "flowchart LR\n C[Text corpus] --> T[Tokenizer]\n T --> I[Token ID stream]\n I --> B[Input/target blocks]\n B --> G[Tiny GPT]\n G --> L[Next-token loss]", concept: "For a sequence of IDs `[a,b,c,d]`, the input can be `[a,b,c]` and target `[b,c,d]`. The model predicts every shifted target position in parallel during training.", code: "text = \"hello tiny language model\"\n# A temporary character-level vocabulary for inspection\nvocab = sorted(set(text))\ntoi = {ch: i for i, ch in enumerate(vocab)}\nids = [toi[ch] for ch in text]\nx, y = ids[:-1], ids[1:]\nprint(x, y)", gotcha: "Never train on text you cannot legally use or that contains secrets. Keep a documented data source, filtering rules, and a held-out validation split.", question: "What are next-token targets for input IDs `[4, 7, 2]`?", options: ["The following IDs in the corpus, such as `[7, 2, next]`.", "Always `[4, 7, 2]` unchanged.", "One text label for the document.", "Random token IDs."], correct: 0, explanation: "Language-model supervision is the same stream shifted one position to the left.", next: "Tokenizers replace this character-level teaching example with efficient subword vocabulary." },
  { slug: "tokenizers-vocabulary", title: "Tokenizers and Vocabulary", subtitle: "Build the bridge from text to compact, reusable token IDs.", minutes: 26, tags: ["BPE", "Tokenization", "Vocabulary"], description: "A tokenizer defines the model's atomic language units. Modern LLMs usually use subwords, balancing a compact vocabulary with the ability to represent rare words and code.", chart: "flowchart LR\n S[Raw text] --> P[Pre-tokenize]\n P --> B[BPE merges]\n B --> ID[Token IDs]\n ID --> D[Decode back to text]", concept: "Byte Pair Encoding begins with small pieces and repeatedly merges frequent adjacent pairs. It discovers reusable units such as common word fragments rather than storing every whole word.", code: "# Inspect a production tokenizer rather than invent token IDs\nimport tiktoken\nenc = tiktoken.get_encoding(\"gpt2\")\nids = enc.encode(\"Tokenization makes language numeric.\")\nprint(ids)\nprint(enc.decode(ids))\nprint([enc.decode([i]) for i in ids])", gotcha: "Token counts are not word counts. Prompt length, model context limits, and pricing must be calculated with the tokenizer used by the chosen model.", question: "Why do LLMs commonly use subword tokenizers?", options: ["They represent rare words using reusable pieces while keeping vocabulary manageable.", "They make every word exactly one token.", "They remove the need to decode output.", "They guarantee semantic understanding."], correct: 0, explanation: "Subwords avoid an enormous whole-word vocabulary and reduce unknown-token problems.", next: "Token IDs become trainable vector representations through embedding tables." },
  { slug: "token-position-embeddings", title: "Token and Position Embeddings", subtitle: "Turn discrete IDs into vectors while preserving order.", minutes: 24, tags: ["Embeddings", "Positions", "Context window"], description: "An embedding table maps every token ID to a learned dense vector. Position embeddings tell the model where each vector sits in the sequence, since attention alone has no order.", chart: "flowchart LR\n I[Token IDs] --> TE[Token embedding lookup]\n P[Positions 0..T-1] --> PE[Position embedding lookup]\n TE --> A[Add vectors]\n PE --> A\n A --> X[Transformer input]", concept: "For batch size B, context length T, and model width C, embeddings have shape `(B,T,C)`. Token and position vectors are added elementwise before the first Transformer block.", code: "import torch\nimport torch.nn as nn\n\nvocab_size, block_size, n_embd = 5000, 128, 64\ntok_emb = nn.Embedding(vocab_size, n_embd)\npos_emb = nn.Embedding(block_size, n_embd)\nids = torch.randint(vocab_size, (4, 16))\npos = torch.arange(ids.size(1))\nx = tok_emb(ids) + pos_emb(pos)\nprint(x.shape)  # [4, 16, 64]", gotcha: "Position IDs must not exceed the embedding table's context window. If you change `block_size`, update every positional component and test long inputs.", question: "Why add position embeddings to token embeddings?", options: ["Attention needs an explicit signal to distinguish token order.", "They convert embeddings back into IDs.", "They replace the vocabulary.", "They remove all padding."], correct: 0, explanation: "Without positions, swapping tokens changes their set but gives attention no direct order information.", next: "Causal self-attention mixes these embeddings while preventing future-token leakage." },
  { slug: "causal-self-attention", title: "Causal Self-Attention", subtitle: "Implement the masked attention that lets GPT predict only from the past.", minutes: 30, tags: ["Self-attention", "Causal mask", "QKV"], description: "GPT uses self-attention: queries, keys, and values all come from its current token sequence. A causal mask ensures each position can attend only to itself and earlier tokens.", chart: "flowchart LR\n X[Token representations] --> Q[Queries]\n X --> K[Keys]\n X --> V[Values]\n Q --> S[Q K transpose / scale]\n K --> S\n S --> M[Apply lower-triangular mask]\n M --> W[Softmax]\n W --> O[Weighted values]\n V --> O", concept: "At position t, the model can use tokens 0 through t, never t+1 onward. This matches next-token generation, where only the existing prefix is known.", code: "import torch\nimport torch.nn.functional as F\n\ndef causal_attention(x):\n    B, T, C = x.shape\n    q, k, v = x, x, x\n    scores = q @ k.transpose(-2, -1) / (C ** 0.5)\n    mask = torch.tril(torch.ones(T, T, device=x.device, dtype=torch.bool))\n    weights = scores.masked_fill(~mask, float(\"-inf\")).softmax(dim=-1)\n    return weights @ v\n\nprint(causal_attention(torch.randn(2, 8, 16)).shape)", gotcha: "Mask before softmax. If future positions receive probability first and are zeroed later, remaining weights no longer form a correct probability distribution.", question: "Why is the causal mask lower triangular?", options: ["Each position can access only itself and earlier positions, never future ones.", "It makes all attention scores equal.", "It selects only the final token.", "It removes positional embeddings."], correct: 0, explanation: "Rows are query positions and columns are key positions; later columns are blocked for each earlier row.", next: "Multi-head attention lets the model learn several context-routing patterns at once." },
  { slug: "multi-head-transformer-block", title: "Multi-Head Attention and a Transformer Block", subtitle: "Combine parallel heads, residual paths, LayerNorm, and an MLP.", minutes: 30, tags: ["Multi-head", "Transformer block", "LayerNorm"], description: "A GPT block uses multiple attention heads in parallel, then adds a feed-forward network. Residual connections and LayerNorm keep the stack trainable.", chart: "flowchart TD\n X[Input] --> N1[LayerNorm]\n N1 --> H[Multi-head causal attention]\n H --> R1[Add residual]\n X --> R1\n R1 --> N2[LayerNorm]\n N2 --> F[MLP: expand, GELU, project]\n F --> R2[Add residual]\n R1 --> R2", concept: "Heads split model width into smaller subspaces and are concatenated after attention. The MLP applies a nonlinear transform per token; attention handles communication between tokens.", code: "import torch.nn as nn\nclass Block(nn.Module):\n    def __init__(self, n_embd=64, n_head=4):\n        super().__init__()\n        self.ln1, self.ln2 = nn.LayerNorm(n_embd), nn.LayerNorm(n_embd)\n        self.attn = nn.MultiheadAttention(n_embd, n_head, batch_first=True)\n        self.mlp = nn.Sequential(nn.Linear(n_embd, 4*n_embd), nn.GELU(), nn.Linear(4*n_embd, n_embd))\n    def forward(self, x, mask):\n        x = x + self.attn(self.ln1(x), self.ln1(x), self.ln1(x), attn_mask=mask, need_weights=False)[0]\n        return x + self.mlp(self.ln2(x))", gotcha: "Be careful with PyTorch mask conventions: boolean mask meaning and mask shape differ between APIs. Write a unit test proving that changing future tokens cannot change earlier logits.", question: "What two jobs are separated inside a Transformer block?", options: ["Attention mixes information across tokens; the MLP transforms each token independently.", "Tokenization and decoding.", "Training and evaluation only.", "GPU and CPU operations."], correct: 0, explanation: "This alternation is the core Transformer pattern: global communication followed by local nonlinear computation.", next: "Stack blocks, add an output head, and connect the model to cross-entropy loss." },
  { slug: "gpt-model-logits-loss", title: "Build a Tiny GPT Model and Loss", subtitle: "Stack blocks and train every position to predict its next token.", minutes: 30, tags: ["GPT", "Logits", "Cross entropy"], description: "A Tiny GPT combines token/position embeddings, a stack of Transformer blocks, final normalization, and a vocabulary projection head that produces logits at every sequence position.", chart: "flowchart LR\n I[Input IDs B x T] --> E[Token + position embeddings]\n E --> B[Stack of GPT blocks]\n B --> N[Final LayerNorm]\n N --> H[Linear vocabulary head]\n H --> L[Logits B x T x V]\n L --> C[Cross-entropy with shifted targets]", concept: "Cross-entropy compares each position's vocabulary logits to the next true token ID. Flatten `(B,T,V)` logits and `(B,T)` targets to compute a single mean loss.", code: "import torch.nn.functional as F\n# logits: B, T, vocab; targets: B, T\nlogits = torch.randn(4, 16, 5000)\ntargets = torch.randint(5000, (4, 16))\nloss = F.cross_entropy(logits.reshape(-1, logits.size(-1)), targets.reshape(-1))\nprint(loss.item())", gotcha: "Do not apply softmax before `F.cross_entropy`; it expects raw logits and includes a numerically stable log-softmax internally.", question: "What is the shape of Tiny GPT output logits before loss?", options: ["(batch, context_length, vocabulary_size)", "(batch, vocabulary_size) only", "(vocabulary_size, context_length)", "One scalar per batch."], correct: 0, explanation: "GPT predicts a probability distribution over the vocabulary at every input position.", next: "Create an efficient batch loader and train this model with a disciplined loop." },
  { slug: "gpt-training-loop", title: "Train a Tiny GPT", subtitle: "Batch token streams, optimize cross-entropy, and monitor validation loss.", minutes: 30, tags: ["Training", "AdamW", "Validation"], description: "Training repeatedly samples a contiguous input block and its shifted targets, computes loss, backpropagates, and updates parameters. Validation loss is the signal for generalization.", chart: "flowchart LR\n S[Token stream] --> B[Random input/target block]\n B --> F[GPT forward]\n F --> L[Cross-entropy loss]\n L --> BW[Backward]\n BW --> O[AdamW step]\n O --> V[Periodic validation loss]", concept: "Use AdamW, a conservative learning rate, and an evaluation function that switches to `model.eval()` and disables gradients. Start by trying to overfit a tiny portion of text to prove the pipeline works.", code: "optimizer = torch.optim.AdamW(model.parameters(), lr=3e-4, weight_decay=0.1)\nfor step in range(max_steps):\n    xb, yb = get_batch(\"train\")\n    _, loss = model(xb, yb)\n    optimizer.zero_grad(set_to_none=True)\n    loss.backward()\n    torch.nn.utils.clip_grad_norm_(model.parameters(), 1.0)\n    optimizer.step()", gotcha: "Do not evaluate while dropout is still active or gradients are retained. Use `model.eval()` and `torch.no_grad()`, then restore `model.train()`.", question: "What is the best first test for a new LLM training loop?", options: ["Overfit a tiny dataset and verify loss drops dramatically.", "Train on a giant web corpus immediately.", "Only inspect generated text before training.", "Disable validation."], correct: 0, explanation: "A tiny overfit test catches broken batching, targets, loss, gradients, and optimizer wiring before expensive training.", next: "Once trained, sample next-token distributions to generate text." },
  { slug: "gpt-inference-sampling", title: "Inference and Sampling", subtitle: "Generate text with temperature, top-k, and top-p controls.", minutes: 26, tags: ["Inference", "Temperature", "Top-k", "Top-p"], description: "Generation repeatedly feeds the current context to GPT, takes final-position logits, chooses a next token, appends it, and stops after a limit or special token.", chart: "flowchart LR\n P[Current token IDs] --> G[GPT forward]\n G --> L[Last-position logits]\n L --> S[Temperature / top-k / top-p]\n S --> N[Sample next ID]\n N --> A[Append and crop context]\n A --> P", concept: "Temperature changes sharpness; top-k preserves only K highest-logit choices; top-p preserves the smallest high-probability set whose cumulative mass reaches p. Context must be cropped to the model's block size.", code: "@torch.no_grad()\ndef generate(model, ids, max_new_tokens, temperature=1.0):\n    for _ in range(max_new_tokens):\n        logits, _ = model(ids[:, -model.block_size:])\n        next_logits = logits[:, -1] / temperature\n        next_id = torch.multinomial(next_logits.softmax(dim=-1), 1)\n        ids = torch.cat([ids, next_id], dim=1)\n    return ids", gotcha: "Use `model.eval()` and `torch.no_grad()` for generation. Leaving gradient tracking active wastes memory every generated token.", question: "Which logits are used to choose the next token?", options: ["The logits at the final position of the current context.", "The average logits over all positions.", "The first position only.", "The target labels."], correct: 0, explanation: "The final context position predicts the token that should follow the entire prefix.", next: "Finish with a reproducible Tiny GPT project that documents model, data, evaluation, and samples." },
  { slug: "tiny-gpt-capstone", title: "Capstone: Ship Your Tiny GPT", subtitle: "Package a small language model with tests, checkpoints, and an honest model card.", minutes: 34, tags: ["Capstone", "Checkpoint", "Evaluation"], description: "The capstone turns your implementation into a credible project: train it on a permitted corpus, save checkpoints, evaluate held-out loss, generate samples, and document limitations.", chart: "flowchart LR\n D[Permitted corpus] --> T[Train / validation tokens]\n T --> M[Tiny GPT training]\n M --> C[Checkpoint + config]\n C --> E[Validation loss + samples]\n E --> R[README / model card]", concept: "A reproducible model artifact needs more than weights: tokenizer identity, vocabulary/configuration, training settings, data provenance, evaluation result, and known limitations.", code: "checkpoint = {\n    \"model\": model.state_dict(),\n    \"optimizer\": optimizer.state_dict(),\n    \"config\": {\"vocab_size\": vocab_size, \"block_size\": block_size},\n    \"step\": step,\n    \"validation_loss\": float(val_loss),\n}\ntorch.save(checkpoint, \"tiny-gpt-checkpoint.pt\")", gotcha: "Do not claim a tiny model understands facts simply because it can complete local patterns. Evaluate on held-out text and document repetition, hallucination, bias, and limited-context failure modes.", question: "What must accompany weights to reload a Tiny GPT correctly?", options: ["The architecture config and the exact tokenizer/vocabulary.", "Only a generated sample.", "The user's prompt history only.", "No other files."], correct: 0, explanation: "Weights have no meaningful shape or token interpretation without the architecture parameters and tokenizer mapping used during training.", next: "You now understand the entire GPT pipeline. Continue to LLM Engineering to make model behavior reliable in real applications." },
];

function sections(s: Spec): Section[] { const offset = s.install ? 1 : 0; return [
  { step: 1, title: "What it means", blocks: [{ type: "text", content: s.description }] },
  ...(s.install ? [{ step: 2, title: "Install it", blocks: [{ type: "code" as const, language: "bash", label: "Windows PowerShell", code: s.install }, { type: "callout" as const, kind: "tip" as const, content: "Start with CPU or a modest GPU and a small corpus. Tiny GPT is for learning the pipeline; memory and training time grow quickly with model width, layers, and context length." }] }] : []),
  { step: 2 + offset, title: "Visual explanation", blocks: [{ type: "diagram", chart: s.chart, label: `${s.title} in context` }] },
  { step: 3 + offset, title: "Core concept", blocks: [{ type: "callout", kind: "insight", content: s.concept }] },
  { step: 4 + offset, title: "Build it", blocks: [{ type: "code", language: "python", label: "Minimal implementation", code: s.code }] },
  { step: 5 + offset, title: "Experiment", blocks: [{ type: "text", content: "First run the component with a tiny fixed input. Print shapes and values, then compare one controlled change at a time. Save train/validation loss and sample outputs while training." }] },
  { step: 6 + offset, title: "Common mistake", blocks: [{ type: "callout", kind: "gotcha", content: s.gotcha }] },
  { step: 7 + offset, title: "Test yourself", blocks: [{ type: "quiz", question: s.question, options: s.options, correct: s.correct, explanation: s.explanation }] },
  { step: 8 + offset, title: "What is next", blocks: [{ type: "text", content: s.next }] },
]; }

export const llmFromScratchLessons: Lesson[] = specs.map((s, index) => ({ slug: s.slug, trackSlug: "llm-from-scratch", order: index + 1, minutes: s.minutes, title: s.title, subtitle: s.subtitle, tags: s.tags, sections: sections(s) }));
