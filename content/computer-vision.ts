import type { Lesson, Section } from "@/lib/content";

type Spec = { slug: string; title: string; subtitle: string; minutes: number; tags: string[]; description: string; chart: string; install?: string; code: string; concept: string; gotcha: string; question: string; options: string[]; correct: number; explanation: string; next: string };

const specs: Spec[] = [
  { slug: "cv-setup-images-tensors", title: "Computer Vision Setup and Image Tensors", subtitle: "Install the tooling and turn pixels into model-ready tensors.", minutes: 20, tags: ["Installation", "Images", "RGB", "Tensors"], description: "Computer-vision models do not see files or pixels as colors. They receive numerical tensors whose shape, channel order, scale, and normalization must match the model's expectations.", install: "# In an activated Python virtual environment\npython -m pip install torch torchvision pillow matplotlib\n\n# Verify the install\npython -c \"import torch, torchvision; print(torch.__version__, torchvision.__version__)\"", chart: "flowchart LR\n F[Image file] --> P[PIL image]\n P --> T[Tensor C x H x W]\n T --> N[Normalize channels]\n N --> M[Vision model]", code: "from PIL import Image\nfrom torchvision import transforms\n\nimage = Image.open(\"photo.jpg\").convert(\"RGB\")\npreprocess = transforms.Compose([transforms.Resize((224, 224)), transforms.ToTensor()])\nx = preprocess(image)\nprint(x.shape, x.min().item(), x.max().item())  # [3, 224, 224], usually 0..1", concept: "An RGB image becomes three channels: red, green, and blue. PyTorch vision convention is `(channels, height, width)`; a batch adds a leading dimension `(batch, channels, height, width)`.", gotcha: "Do not feed HWC NumPy arrays directly to a PyTorch CNN expecting CHW tensors. Transpose or use `ToTensor`, and preserve the preprocessing used in training.", question: "What is the usual PyTorch tensor shape for a batch of 32 RGB 224x224 images?", options: ["(32, 3, 224, 224)", "(32, 224, 224, 3)", "(3, 32, 224, 224)", "(224, 224, 3)"], correct: 0, explanation: "PyTorch uses NCHW order: batch, channels, height, width.", next: "Now learn the local pattern detector that made modern vision practical: convolution." },
  { slug: "convolution-kernels-stride-padding", title: "Convolution, Kernels, Stride, and Padding", subtitle: "Learn how filters detect visual patterns while controlling image size.", minutes: 26, tags: ["Convolution", "Kernels", "Stride", "Padding"], description: "A convolutional filter scans a small window across an image. During training its values become detectors for useful local patterns such as edges, textures, or parts of objects.", chart: "flowchart LR\n I[Input image] --> K[Small learnable kernel]\n K --> S[Slide across image]\n S --> F[Feature map]\n P[Stride and padding] -. control size .-> S", code: "import torch\nimport torch.nn as nn\n\nx = torch.randn(1, 3, 32, 32)\nconv = nn.Conv2d(in_channels=3, out_channels=16, kernel_size=3, stride=1, padding=1)\ny = conv(x)\nprint(y.shape)  # [1, 16, 32, 32]", concept: "Output size per spatial axis is floor((input + 2*padding - kernel)/stride) + 1. More output channels mean more filters and more learned feature maps.", gotcha: "Padding changes border behavior. A 3x3 convolution with padding=1 and stride=1 preserves height and width; omitting it shrinks both by two.", question: "What does a convolution kernel learn?", options: ["A local pattern detector whose weights are optimized from data.", "A fixed list of object labels.", "The image file format.", "Only the image dimensions."], correct: 0, explanation: "The same small learned filter is reused at every location, which captures local patterns efficiently.", next: "Pooling and deeper stacks turn these local features into robust representations." },
  { slug: "pooling-and-cnns", title: "Pooling and Convolutional Neural Networks", subtitle: "Build the hierarchy from edges to object-level features.", minutes: 26, tags: ["Pooling", "CNN", "Feature maps"], description: "CNNs stack convolution, activation, and pooling blocks. Early layers often find edges; deeper layers combine them into textures, shapes, and class-relevant patterns.", chart: "flowchart LR\n I[Image] --> C1[Conv + ReLU]\n C1 --> P1[Pool]\n P1 --> C2[Conv + ReLU]\n C2 --> G[Global average pool]\n G --> H[Classification head]", code: "import torch.nn as nn\n\ncnn = nn.Sequential(\n    nn.Conv2d(3, 16, 3, padding=1), nn.ReLU(), nn.MaxPool2d(2),\n    nn.Conv2d(16, 32, 3, padding=1), nn.ReLU(), nn.AdaptiveAvgPool2d(1),\n    nn.Flatten(), nn.Linear(32, 10),\n)", concept: "Pooling reduces spatial resolution, making later computation cheaper and features less sensitive to tiny position changes. Adaptive average pooling creates a fixed-size representation from varying image sizes.", gotcha: "Do not flatten a large feature map too early. It creates an enormous fully connected layer; downsample gradually or use global average pooling.", question: "What is pooling mainly used for in a CNN?", options: ["Reduce spatial resolution while retaining salient local responses.", "Turn labels into one-hot vectors.", "Increase every image's color depth.", "Calculate gradients."], correct: 0, explanation: "Pooling compresses feature maps and provides some tolerance to small translations.", next: "Instead of training this hierarchy from zero, reuse a pretrained one through transfer learning." },
  { slug: "vision-transfer-learning", title: "Transfer Learning for Vision", subtitle: "Fine-tune a pretrained image model on your own labelled data.", minutes: 28, tags: ["Transfer learning", "ResNet", "Fine-tuning"], description: "Pretrained vision networks already encode useful visual features. With modest data, replacing their final head is often faster and more accurate than training a new CNN from scratch.", chart: "flowchart LR\n B[Pretrained ResNet backbone] --> F[General visual features]\n F --> H[New classification head]\n H --> D[Your classes]\n D --> FT[Optionally fine-tune backbone]", code: "from torchvision.models import resnet18, ResNet18_Weights\nimport torch.nn as nn\n\nweights = ResNet18_Weights.DEFAULT\nmodel = resnet18(weights=weights)\nfor p in model.parameters(): p.requires_grad = False\nmodel.fc = nn.Linear(model.fc.in_features, num_classes)\npreprocess = weights.transforms()", concept: "First freeze the backbone and train the new head. If validation performance plateaus, unfreeze selected upper layers and fine-tune using a smaller learning rate.", gotcha: "Use the preprocessing attached to pretrained weights. Wrong resize, crop, channel scale, or normalization can make a strong pretrained model appear broken.", question: "Why freeze the backbone initially?", options: ["To preserve useful pretrained features while the randomly initialized task head stabilizes.", "To remove all model parameters.", "To avoid needing labels.", "To make images grayscale."], correct: 0, explanation: "A new head needs to learn the new labels; freezing protects the backbone from large, noisy early updates.", next: "Classification gives one label per image. Object detection adds location and multiple objects." },
  { slug: "object-detection", title: "Object Detection", subtitle: "Find every target object and draw a box around it.", minutes: 24, tags: ["Detection", "Bounding boxes", "IoU"], description: "Object detection predicts both what is in an image and where it is. Each detection has a class, a confidence score, and a bounding box.", chart: "flowchart LR\n I[Image] --> B[Backbone features]\n B --> R[Region / detection head]\n R --> C[Class scores]\n R --> BX[Box coordinates]\n C --> N[NMS: remove duplicate boxes]\n BX --> N", code: "from torchvision.models.detection import fasterrcnn_resnet50_fpn\nimport torch\n\nmodel = fasterrcnn_resnet50_fpn(weights=\"DEFAULT\").eval()\nwith torch.no_grad():\n    predictions = model([torch.rand(3, 480, 640)])\nprint(predictions[0].keys())  # boxes, labels, scores", concept: "Intersection over Union (IoU) measures box overlap: intersection area divided by union area. Non-maximum suppression keeps the best box among overlapping duplicate detections.", gotcha: "Detection models commonly receive a list of variable-sized image tensors, not one pre-stacked batch tensor. Read the chosen model's input contract.", question: "What does IoU measure?", options: ["The overlap between predicted and ground-truth bounding boxes.", "The number of detected classes.", "The image resolution.", "The optimizer's learning rate."], correct: 0, explanation: "IoU quantifies localization quality and is used for matching predictions to ground truth.", next: "Segmentation goes further by predicting a class for every pixel." },
  { slug: "image-segmentation", title: "Image Segmentation", subtitle: "Label each pixel for tasks such as medical images and scene understanding.", minutes: 24, tags: ["Segmentation", "Masks", "U-Net"], description: "Segmentation assigns a class or object identity to every image pixel. It is used where location detail matters more than a single image label.", chart: "flowchart LR\n I[Image] --> E[Encoder: compress features]\n E --> D[Decoder: restore resolution]\n D --> M[Per-pixel class mask]\n S[Skip connections] --> D", code: "import torch\nimport torch.nn.functional as F\n\nlogits = torch.randn(2, 4, 256, 256)  # N, classes, H, W\nmasks = torch.randint(0, 4, (2, 256, 256))\nloss = F.cross_entropy(logits, masks)\npred_mask = logits.argmax(dim=1)", concept: "Semantic segmentation predicts a category at each pixel; instance segmentation distinguishes separate objects of the same category. Encoder-decoder models recover detail after downsampling.", gotcha: "For multiclass segmentation, target masks are integer class IDs with shape `(N,H,W)`, not RGB images or one-hot tensors in the usual CrossEntropyLoss setup.", question: "What is the output of semantic segmentation?", options: ["A class prediction for every pixel.", "One label for the whole image.", "Only bounding-box coordinates.", "A text transcript."], correct: 0, explanation: "Segmentation preserves spatial detail by assigning labels across the full image grid.", next: "OCR applies vision to documents, but its output is text rather than a visual label." },
  { slug: "ocr-document-vision", title: "OCR and Document Vision", subtitle: "Extract text and structure from scans, photos, and invoices.", minutes: 22, tags: ["OCR", "Documents", "Preprocessing"], description: "Optical character recognition converts visual text into machine-readable strings. Production document pipelines also handle layout, rotation, confidence, and field extraction.", chart: "flowchart LR\n D[Document image] --> Q[Deskew / enhance]\n Q --> T[Text detection]\n T --> R[Text recognition]\n R --> P[Post-process fields]\n P --> O[Structured output]", code: "from PIL import Image, ImageOps\n\nimage = Image.open(\"invoice.jpg\").convert(\"L\")\n# Simple preprocessing before an OCR engine or model\nimage = ImageOps.autocontrast(image)\nimage = image.resize((image.width * 2, image.height * 2))\nimage.save(\"prepared-invoice.png\")", concept: "OCR often separates text detection (where text appears) from recognition (what the characters say). Better input quality—orientation, contrast, resolution—often improves results before any model tuning.", gotcha: "Do not treat OCR output as ground truth. Keep confidence values, validate important fields, and retain the source region for human review.", question: "Why preprocess a document before OCR?", options: ["It can improve text visibility, orientation, and recognition accuracy.", "It removes the need for a recognition model.", "It guarantees every field is correct.", "It turns text into labels."], correct: 0, explanation: "Deskewing, contrast correction, and adequate resolution reduce visual noise that makes characters ambiguous.", next: "Vision Transformers replace convolutions with attention over image patches." },
  { slug: "vision-transformers", title: "Vision Transformers", subtitle: "Process image patches with Transformer attention.", minutes: 26, tags: ["ViT", "Transformers", "Image patches"], description: "Vision Transformers split an image into patches, embed them as tokens, and use self-attention to combine information across the whole image.", chart: "flowchart LR\n I[Image] --> P[Split into patches]\n P --> E[Patch embeddings + positions]\n E --> T[Transformer encoder blocks]\n T --> H[Classification or vision head]", code: "from torchvision.models import vit_b_16, ViT_B_16_Weights\nimport torch\n\nweights = ViT_B_16_Weights.DEFAULT\nmodel = vit_b_16(weights=weights).eval()\nx = weights.transforms()(Image.open(\"photo.jpg\").convert(\"RGB\")).unsqueeze(0)\nwith torch.no_grad():\n    logits = model(x)\nprint(logits.shape)", concept: "A 224x224 image with 16x16 patches produces 14x14 = 196 patch tokens. Attention lets every patch exchange information with every other patch, unlike a strictly local convolution.", gotcha: "Vision Transformers often need more data or pretrained weights than small CNNs. Fine-tuning a pretrained ViT is usually the practical starting point.", question: "What becomes a token in a Vision Transformer?", options: ["A fixed-size image patch embedded as a vector.", "A single RGB channel.", "A bounding box only.", "The optimizer state."], correct: 0, explanation: "ViT treats patches analogously to word tokens, then uses positional information and attention to model relationships.", next: "You now have a complete map of modern vision tasks: classify, localize, segment, read documents, and use attention-based models." },
];

function sections(s: Spec): Section[] { const offset = s.install ? 1 : 0; return [
  { step: 1, title: "What it means", blocks: [{ type: "text", content: s.description }] },
  ...(s.install ? [{ step: 2, title: "Install it", blocks: [{ type: "code" as const, language: "bash", label: "Terminal", code: s.install }, { type: "callout" as const, kind: "tip" as const, content: "Use an isolated virtual environment. For GPU-specific PyTorch builds, choose the current command from the official PyTorch installer." }] }] : []),
  { step: 2 + offset, title: "Visual explanation", blocks: [{ type: "diagram", chart: s.chart, label: `${s.title} in context` }] },
  { step: 3 + offset, title: "Core concept", blocks: [{ type: "callout", kind: "insight", content: s.concept }] },
  { step: 4 + offset, title: "Run it", blocks: [{ type: "code", language: "python", label: "Minimal example", code: s.code }] },
  { step: 5 + offset, title: "Experiment", blocks: [{ type: "text", content: "Run the example with a small real image or synthetic tensor. Print shapes and intermediate outputs; then change one parameter and explain its effect." }] },
  { step: 6 + offset, title: "Common mistake", blocks: [{ type: "callout", kind: "gotcha", content: s.gotcha }] },
  { step: 7 + offset, title: "Test yourself", blocks: [{ type: "quiz", question: s.question, options: s.options, correct: s.correct, explanation: s.explanation }] },
  { step: 8 + offset, title: "What is next", blocks: [{ type: "text", content: s.next }] },
]; }

export const computerVisionLessons: Lesson[] = specs.map((s, index) => ({ slug: s.slug, trackSlug: "computer-vision", order: index + 1, minutes: s.minutes, title: s.title, subtitle: s.subtitle, tags: s.tags, sections: sections(s) }));
