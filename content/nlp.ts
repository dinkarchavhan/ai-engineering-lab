import type { Lesson, Section } from "@/lib/content";

type Spec = { slug: string; title: string; subtitle: string; minutes: number; tags: string[]; description: string; diagram: string; install?: string; concept: string; code: string; gotcha: string; question: string; options: string[]; correct: number; explanation: string; next: string };

const specs: Spec[] = [
  { slug: "nlp-setup-text-preprocessing", title: "NLP Setup and Text Preprocessing", subtitle: "Install the basics and turn messy text into a consistent input.", minutes: 20, tags: ["Installation", "Text cleaning", "Preprocessing"], description: "Language arrives as inconsistent strings: mixed casing, URLs, punctuation, markup, emojis, and typos. Preprocessing makes the representation consistent while preserving the signal your task needs.", install: "python -m venv .venv\n# Windows PowerShell:\n.\\.venv\\Scripts\\Activate.ps1\npython -m pip install --upgrade pip\npython -m pip install scikit-learn nltk spacy torch\n# Optional language model for spaCy:\npython -m spacy download en_core_web_sm", diagram: "flowchart LR\n R[Raw text] --> C[Clean / normalize]\n C --> T[Tokenize]\n T --> V[Vectorize]\n V --> M[ML or neural model]", concept: "Preprocessing is task-dependent. Lowercasing may help topic classification but can harm named-entity recognition, where capitalization carries useful information.", code: "import re\n\ndef clean_text(text: str) -> str:\n    text = text.lower().strip()\n    text = re.sub(r\"https?://\\S+\", \" <URL> \", text)\n    text = re.sub(r\"\\s+\", \" \", text)\n    return text\n\nprint(clean_text(\"  Great product! https://example.com  \"))", gotcha: "Do not delete information automatically. Negation, punctuation, casing, and emojis can matter; keep a raw-text column so transformations are reversible and testable.", question: "Why should preprocessing be chosen per task?", options: ["Different transformations can remove or preserve different useful signals.", "All NLP models require lowercase text.", "It guarantees perfect labels.", "It replaces tokenization."], correct: 0, explanation: "A transformation is only useful if it improves the target task; it can also remove important context.", next: "Once text is cleaned, tokenization decides the units a model actually reads." },
  { slug: "tokenization", title: "Tokenization", subtitle: "Split text into the words, subwords, or symbols a model can process.", minutes: 22, tags: ["Tokenization", "Vocabulary", "Subwords"], description: "Models cannot consume strings directly. Tokenization maps text to units and then integer IDs, creating the bridge from language to numerical computation.", diagram: "flowchart LR\n S[Sentence] --> T[Tokenizer]\n T --> U[Tokens / subwords]\n U --> I[Token IDs]\n I --> E[Embedding vectors]", concept: "Word tokenizers are easy to read but struggle with unknown words. Subword tokenizers split rare words into reusable pieces, which is why modern language models can handle open vocabularies.", code: "import re\n\ndef simple_tokenize(text: str):\n    return re.findall(r\"\\b\\w+\\b|[^\\w\\s]\", text.lower())\n\ntext = \"Tokenizers handle don't, numbers (42), and punctuation!\"\nprint(simple_tokenize(text))", gotcha: "Never assume two models use interchangeable token IDs. Token IDs have meaning only with the tokenizer and vocabulary that created them.", question: "Why are subword tokenizers useful?", options: ["They represent rare or unseen words using reusable smaller pieces.", "They eliminate the need for a vocabulary.", "They always create one token per sentence.", "They convert tokens directly into labels."], correct: 0, explanation: "Breaking words into pieces reduces unknown-word problems while keeping the vocabulary manageable.", next: "Classical NLP starts by counting token patterns with bag-of-words and TF-IDF." },
  { slug: "ngrams-tfidf", title: "N-Grams and TF-IDF", subtitle: "Represent documents with interpretable word and phrase importance.", minutes: 24, tags: ["Bag of words", "N-grams", "TF-IDF"], description: "Before embeddings, many NLP systems represented documents using token counts. TF-IDF remains an excellent, fast baseline for search and text classification.", diagram: "flowchart LR\n D[Documents] --> V[Vocabulary]\n V --> C[Token / n-gram counts]\n C --> F[TF-IDF feature matrix]\n F --> CL[Classifier or similarity search]", concept: "Term frequency captures how often a term appears in a document. Inverse document frequency downweights terms appearing in almost every document. N-grams preserve short phrases such as 'not good'.", code: "from sklearn.feature_extraction.text import TfidfVectorizer\n\ndocs = [\"great support and fast shipping\", \"slow shipping but helpful support\"]\nvectorizer = TfidfVectorizer(ngram_range=(1, 2), stop_words=\"english\")\nX = vectorizer.fit_transform(docs)\nprint(X.shape, vectorizer.get_feature_names_out()[:6])", gotcha: "Fit the vectorizer only on training documents. Fitting it on validation or test text leaks vocabulary and document-frequency information.", question: "What does IDF reduce the influence of?", options: ["Terms that occur in many documents and are less discriminative.", "Rare terms only.", "All phrases longer than one word.", "Class labels."], correct: 0, explanation: "A common word carries little information for distinguishing one document from another, so IDF gives it a lower weight.", next: "Stemming and lemmatization offer another way to reduce surface variation in words." },
  { slug: "stemming-lemmatization", title: "Stemming and Lemmatization", subtitle: "Normalize related word forms while understanding the trade-offs.", minutes: 18, tags: ["Stemming", "Lemmatization", "Normalization"], description: "Words such as 'running', 'runs', and 'ran' often express related meaning. Normalization can reduce vocabulary size and improve matching, but overly aggressive rules can damage meaning.", diagram: "flowchart LR\n W[Words: runs, running, ran] --> S[Stemmer]\n S --> R1[run-like root]\n W --> L[Lemmatizer + POS]\n L --> R2[Dictionary lemma: run]", concept: "Stemming applies heuristic chopping and may return non-words. Lemmatization uses vocabulary and grammatical context to return a valid base form, often at greater computational cost.", code: "from nltk.stem import PorterStemmer, WordNetLemmatizer\n\nstemmer = PorterStemmer()\nlemmatizer = WordNetLemmatizer()\nfor word in [\"studies\", \"studying\", \"better\"]:\n    print(word, \"->\", stemmer.stem(word), \"|\", lemmatizer.lemmatize(word))", gotcha: "A lemmatizer needs the correct part of speech for its best result. Treating every word as a noun can leave verbs unchanged or give surprising lemmas.", question: "What is a key advantage of lemmatization over stemming?", options: ["It aims to return a valid dictionary base form using linguistic information.", "It requires no vocabulary.", "It always preserves exact word form.", "It produces embeddings."], correct: 0, explanation: "Stemmers cut strings by rules; lemmatizers target canonical words and can use grammatical context.", next: "Dense embeddings replace sparse counts with vectors that encode distributional meaning." },
  { slug: "word-embeddings", title: "Word Embeddings: Word2Vec and GloVe", subtitle: "Represent words as dense vectors whose geometry carries meaning.", minutes: 26, tags: ["Embeddings", "Word2Vec", "GloVe", "Vectors"], description: "Embeddings map each token to a learned dense vector. Words used in similar contexts tend to live near one another in this vector space.", diagram: "flowchart LR\n T[Token ID] --> E[Embedding lookup table]\n E --> V[Dense vector]\n V --> S[Similarity / downstream model]\n C[Context words] -. training signal .-> E", concept: "Word2Vec learns embeddings from local context; GloVe learns from global co-occurrence statistics. Both express useful similarity, but static embeddings give one vector to a word regardless of its sentence context.", code: "import numpy as np\n\ndef cosine(a, b):\n    return float(a @ b / (np.linalg.norm(a) * np.linalg.norm(b)))\n\n# In practice these vectors are learned or loaded from a pretrained model.\nking = np.array([0.8, 0.1, 0.3])\nqueen = np.array([0.75, 0.12, 0.28])\nprint(cosine(king, queen))", gotcha: "Do not interpret embedding dimensions individually as human-readable features. Meaning appears in directions and distances across the full vector space.", question: "What does it mean when two embedding vectors have high cosine similarity?", options: ["They point in similar directions and often represent related usage or meaning.", "They are the same token ID.", "They must have the same word length.", "They are guaranteed synonyms."], correct: 0, explanation: "Cosine similarity compares vector direction. It is a useful semantic signal, not proof of exact equivalence.", next: "Use embeddings to build a practical semantic-similarity system." },
  { slug: "semantic-similarity", title: "Semantic Similarity", subtitle: "Retrieve text by meaning instead of only exact word overlap.", minutes: 22, tags: ["Similarity", "Cosine", "Retrieval"], description: "Semantic similarity ranks texts that express related ideas even when their wording differs. It powers recommendations, duplicate detection, semantic search, and retrieval pipelines.", diagram: "flowchart LR\n Q[Query text] --> EQ[Query embedding]\n D[Document texts] --> ED[Document embeddings]\n EQ --> C[Cosine similarity]\n ED --> C\n C --> R[Ranked relevant documents]", concept: "Cosine similarity divides the dot product by vector lengths, focusing on direction. For sentence-level similarity, use a sentence embedding model rather than averaging unrelated word vectors when quality matters.", code: "import torch\nimport torch.nn.functional as F\n\nquery = torch.tensor([[1.0, 2.0, 1.0]])\ndocs = torch.tensor([[1.0, 1.8, 1.0], [-1.0, 0.0, 2.0]])\nscores = F.cosine_similarity(query, docs)\nprint(scores, scores.argsort(descending=True))", gotcha: "Use a normalized, task-appropriate embedding model. Comparing vectors from different models or inconsistent preprocessing yields meaningless scores.", question: "Why normalize vectors when using cosine similarity?", options: ["It makes similarity depend on direction rather than vector magnitude.", "It changes text into tokens.", "It removes all duplicate documents.", "It trains the embedding model."], correct: 0, explanation: "Cosine is inherently a directional comparison; normalized vectors make dot product ranking equivalent to cosine ranking.", next: "Before attention dominated NLP, sequence models carried context through recurrent hidden states." },
  { slug: "rnn-lstm-gru", title: "RNN, LSTM, and GRU", subtitle: "Model token order by carrying a state through a sequence.", minutes: 28, tags: ["RNN", "LSTM", "GRU", "Sequences"], description: "Recurrent neural networks read sequences step by step, updating a hidden state that summarizes prior tokens. LSTMs and GRUs add gates to retain important information longer.", diagram: "flowchart LR\n X1[Token 1] --> H1[Hidden state]\n H1 --> H2[Hidden state]\n X2[Token 2] --> H2\n H2 --> H3[Hidden state]\n X3[Token 3] --> H3\n H3 --> Y[Prediction]", concept: "A vanilla RNN repeatedly applies h_t = f(Wx_t + Uh_(t-1)). LSTM/GRU gates decide what to keep, forget, and expose, reducing—but not eliminating—long-range gradient problems.", code: "import torch\nimport torch.nn as nn\n\nrnn = nn.GRU(input_size=64, hidden_size=128, batch_first=True)\nx = torch.randn(16, 20, 64)  # batch, tokens, embedding dim\noutputs, final_state = rnn(x)\nprint(outputs.shape, final_state.shape)", gotcha: "With `batch_first=True`, tensor order is `(batch, sequence, features)`. Without it, PyTorch expects `(sequence, batch, features)`—a frequent shape bug.", question: "What problem do LSTM and GRU gates address?", options: ["They help retain useful information across longer sequences.", "They turn images into patches.", "They eliminate tokenization.", "They produce bounding boxes."], correct: 0, explanation: "Gates create paths for information and gradients to persist, reducing vanishing-gradient difficulty in vanilla RNNs.", next: "Attention replaces the single hidden-state bottleneck by letting tokens directly focus on relevant context." },
  { slug: "attention", title: "Attention", subtitle: "Let each token selectively focus on the context it needs.", minutes: 30, tags: ["Attention", "Query", "Key", "Value"], description: "Attention computes a weighted mixture of values, where weights come from how well a query matches keys. It lets a token access distant relevant context without passing everything through one recurrent state.", diagram: "flowchart LR\n X[Token embeddings] --> Q[Queries]\n X --> K[Keys]\n X --> V[Values]\n Q --> S[Q K transpose / scale]\n K --> S\n S --> A[Softmax attention weights]\n A --> O[Weighted values]\n V --> O", concept: "Scaled dot-product attention is softmax(QK-transpose/sqrt(d_k))V. Queries ask what to find, keys describe what is available, and values carry the information to combine.", code: "import torch\nimport torch.nn.functional as F\n\nQ = torch.randn(1, 4, 8); K = torch.randn(1, 4, 8); V = torch.randn(1, 4, 8)\nscores = Q @ K.transpose(-2, -1) / (Q.size(-1) ** 0.5)\nweights = F.softmax(scores, dim=-1)\ncontext = weights @ V\nprint(context.shape)  # [1, 4, 8]", gotcha: "Apply softmax over the key/context dimension (usually the final axis), not over embedding dimensions. Use an attention mask to exclude padding or future tokens when needed.", question: "What do attention weights represent?", options: ["How strongly each query should combine information from each key/value position.", "A fixed vocabulary size.", "Only a token's own embedding.", "The model's learning rate."], correct: 0, explanation: "Each query creates a distribution over context positions, then uses it to take a weighted sum of value vectors.", next: "You now understand the NLP path from token counts to embeddings, recurrence, and attention—the foundation for Transformers and LLMs." },
];

function sections(s: Spec): Section[] { const offset = s.install ? 1 : 0; return [
  { step: 1, title: "What it means", blocks: [{ type: "text", content: s.description }] },
  ...(s.install ? [{ step: 2, title: "Install it", blocks: [{ type: "code" as const, language: "bash", label: "Windows PowerShell", code: s.install }, { type: "callout" as const, kind: "tip" as const, content: "Install packages inside a virtual environment. Download only the language resources you need, and pin versions when reproducing a project." }] }] : []),
  { step: 2 + offset, title: "Visual explanation", blocks: [{ type: "diagram", chart: s.diagram, label: `${s.title} in context` }] },
  { step: 3 + offset, title: "Core concept", blocks: [{ type: "callout", kind: "insight", content: s.concept }] },
  { step: 4 + offset, title: "Run it", blocks: [{ type: "code", language: "python", label: "Minimal example", code: s.code }] },
  { step: 5 + offset, title: "Experiment", blocks: [{ type: "text", content: "Run this with a small set of real sentences. Inspect tokens, shapes, vectors, or scores before changing one setting and recording the impact." }] },
  { step: 6 + offset, title: "Common mistake", blocks: [{ type: "callout", kind: "gotcha", content: s.gotcha }] },
  { step: 7 + offset, title: "Test yourself", blocks: [{ type: "quiz", question: s.question, options: s.options, correct: s.correct, explanation: s.explanation }] },
  { step: 8 + offset, title: "What is next", blocks: [{ type: "text", content: s.next }] },
]; }

export const nlpLessons: Lesson[] = specs.map((s, index) => ({ slug: s.slug, trackSlug: "nlp", order: index + 1, minutes: s.minutes, title: s.title, subtitle: s.subtitle, tags: s.tags, sections: sections(s) }));
