import type { Lesson, Section } from "@/lib/content";

type Spec = { slug: string; title: string; subtitle: string; minutes: number; tags: string[]; description: string; diagram: string; math: string; code: string; install?: string; gotcha: string; question: string; options: string[]; correct: number; explanation: string; next: string };

const specs: Spec[] = [
  { slug: "pytorch-installation", title: "Install PyTorch and Choose a Device", subtitle: "Set up a reproducible CPU or GPU environment.", minutes: 14, tags: ["Installation", "CUDA", "Device"], description: "PyTorch needs a compatible build for your Python version and hardware. Start with a tiny verification script before downloading a dataset or writing a model.", diagram: "flowchart LR\n A[Python environment] --> B[Install matching PyTorch build]\n B --> C{CUDA or MPS available?}\n C -- yes --> D[Accelerated device]\n C -- no --> E[CPU]\n D --> F[Verify a tensor operation]\n E --> F", math: "A device determines where tensor storage and operations live. Inputs and model parameters must be on the same device.", install: "# Windows PowerShell: create and activate an isolated environment\npython -m venv .venv\n.\\.venv\\Scripts\\Activate.ps1\npython -m pip install --upgrade pip\n\n# CPU install (works on every supported machine)\npython -m pip install torch torchvision\n\n# For an NVIDIA GPU, use the current CUDA-specific command from:\n# https://pytorch.org/get-started/locally/\n# It must match your operating system, Python version, and CUDA driver.", code: "import torch\n\nprint(\"PyTorch version:\", torch.__version__)\nprint(\"CUDA available:\", torch.cuda.is_available())\nprint(\"MPS available:\", torch.backends.mps.is_available())\n\ndevice = (\n    \"cuda\" if torch.cuda.is_available()\n    else \"mps\" if torch.backends.mps.is_available()\n    else \"cpu\"\n)\nx = torch.tensor([1.0, 2.0], device=device)\nprint(f\"Running on {device}:\", x * 2)", gotcha: "A CUDA-enabled PyTorch install does not make a CPU-only machine use a GPU. Check `torch.cuda.is_available()` rather than assuming a device.", question: "What must be true before `model(x)` can run on a GPU?", options: ["Both the model parameters and x are on the same GPU device.", "Only x is on the GPU.", "Only the loss is on the GPU.", "CUDA is mentioned in requirements.txt."], correct: 0, explanation: "PyTorch operations require compatible tensors on the same device; move both model and batches with `.to(device)`.", next: "With the environment ready, learn the tensor operations that every model uses." },
  { slug: "tensors-and-operations", title: "Tensors and Operations", subtitle: "Work confidently with shapes, dtypes, broadcasting, and vectorized math.", minutes: 22, tags: ["Tensors", "Shapes", "Dtypes", "Broadcasting"], description: "A tensor is PyTorch's multidimensional array. Neural-network code is mostly tensor transformations, so shape awareness matters more than memorizing APIs.", diagram: "flowchart LR\n A[Batch N x features D] --> B[Linear transform]\n W[Weights D x H] --> B\n B --> C[Batch N x hidden H]\n C --> D[Activation / next layer]", math: "For X with shape (N,D) and W with shape (D,H), `X @ W` has shape (N,H). Broadcasting expands compatible singleton dimensions without explicitly copying data.", code: "import torch\nX = torch.randn(32, 8)\nW = torch.randn(8, 16)\nb = torch.zeros(16)\nhidden = X @ W + b  # b broadcasts over the batch\nprint(hidden.shape, hidden.dtype)  # torch.Size([32, 16])", gotcha: "Do not use `view` on a non-contiguous tensor after operations like transpose; use `reshape`, or call `.contiguous()` first.", question: "What is the shape of `(32, 8) @ (8, 16)`?", options: ["(32, 16)", "(8, 8)", "(32, 8)", "(16, 32)"], correct: 0, explanation: "Matrix multiplication matches the inner dimension 8 and retains the outer dimensions.", next: "Next make tensors track their own derivatives with autograd." },
  { slug: "autograd", title: "Autograd and Gradients", subtitle: "Let PyTorch calculate derivatives through your computation graph.", minutes: 20, tags: ["Autograd", "Gradients", "backward"], description: "Autograd records differentiable tensor operations during the forward pass, then uses the chain rule to populate gradients when you call backward.", diagram: "flowchart LR\n X[Tensor requires_grad] --> F[Forward operations]\n F --> L[Scalar loss]\n L --> B[loss.backward()]\n B --> G[.grad on leaf tensors]", math: "For a scalar loss L, `loss.backward()` computes dL/dtheta for every leaf parameter theta that has `requires_grad=True`.", code: "import torch\nw = torch.tensor(2.0, requires_grad=True)\nloss = (w * 3 - 10) ** 2\nloss.backward()\nprint(loss.item(), w.grad.item())  # d/dw (3w-10)^2", gotcha: "Gradients accumulate. Clear them with `optimizer.zero_grad()` before each new backward pass unless deliberate gradient accumulation is your goal.", question: "Where does autograd store a parameter's computed derivative?", options: ["In the parameter's `.grad` attribute.", "In `.data`.", "Inside the DataLoader.", "Only in the optimizer."], correct: 0, explanation: "Leaf tensors such as model parameters receive their accumulated derivative in `.grad`.", next: "Autograd needs batches, labels, and reliable data delivery—enter Dataset and DataLoader." },
  { slug: "datasets-dataloaders", title: "Datasets and DataLoaders", subtitle: "Feed training data in shuffled, efficient mini-batches.", minutes: 22, tags: ["Dataset", "DataLoader", "Batches"], description: "Datasets define how to retrieve one example; DataLoaders combine examples into batches and handle shuffling, workers, and memory transfer.", diagram: "flowchart LR\n F[Files / arrays] --> DS[Dataset: one indexed example]\n DS --> DL[DataLoader: batches]\n DL --> T[Training loop]\n T --> M[Model]", math: "A batch estimator uses an approximate gradient from B examples instead of the whole dataset. Batches trade gradient noise against memory and throughput.", code: "from torch.utils.data import TensorDataset, DataLoader\nimport torch\n\nX = torch.randn(1000, 8); y = torch.randint(0, 3, (1000,))\ndataset = TensorDataset(X, y)\nloader = DataLoader(dataset, batch_size=64, shuffle=True)\nfor xb, yb in loader:\n    print(xb.shape, yb.shape)\n    break", gotcha: "Do not shuffle a time series when future values must remain unseen. Use a chronological split and a DataLoader with `shuffle=False` for evaluation.", question: "What does a Dataset normally return from `__getitem__`?", options: ["One input example and its target (when supervised).", "The complete training history.", "A trained model.", "Only a batch of gradients."], correct: 0, explanation: "DataLoader handles batching; Dataset defines access to individual indexed samples.", next: "Now define networks cleanly with modules and reusable layers." },
  { slug: "models-and-modules", title: "Models and Modules", subtitle: "Build neural networks from composable PyTorch components.", minutes: 22, tags: ["nn.Module", "Layers", "forward"], description: "`nn.Module` packages parameters, submodules, and the forward computation. It gives optimizers and checkpointing a reliable view of the model.", diagram: "flowchart LR\n X[Input] --> L1[Linear]\n L1 --> A[ReLU]\n A --> L2[Linear]\n L2 --> Z[Logits]", math: "A Linear layer learns W and b and applies xW-transpose+b. A model's parameters are the union of all parameterized submodules.", code: "import torch.nn as nn\n\nclass Classifier(nn.Module):\n    def __init__(self, input_dim=8, classes=3):\n        super().__init__()\n        self.net = nn.Sequential(nn.Linear(input_dim, 32), nn.ReLU(), nn.Linear(32, classes))\n    def forward(self, x):\n        return self.net(x)\n\nmodel = Classifier()\nprint(sum(p.numel() for p in model.parameters()))", gotcha: "Define layers in `__init__`, not inside `forward`. Layers created in forward are recreated every call and are not registered as persistent parameters.", question: "Why must a custom model call `super().__init__()`?", options: ["It initializes Module internals that register layers and parameters.", "It runs the training loop.", "It moves the model to GPU.", "It creates labels."], correct: 0, explanation: "Module registration lets `.parameters()`, `.to()`, and `state_dict()` discover the model's contents.", next: "The model exists; next connect it to loss and optimization in a complete training loop." },
  { slug: "training-loops", title: "Training Loops", subtitle: "Train a model with forward pass, loss, backward pass, and optimizer step.", minutes: 28, tags: ["Training loop", "Optimizer", "Loss"], description: "A training loop is the central repeatable procedure of deep learning. Every batch follows the same order and every omitted step has a recognizable failure mode.", diagram: "flowchart LR\n B[Mini-batch] --> F[model(x)]\n F --> L[criterion(logits, y)]\n L --> Z[zero gradients]\n Z --> BW[backward]\n BW --> S[optimizer.step]\n S --> B", math: "Training minimizes an average loss over batches. An optimizer uses the gradient from backward to update each learnable parameter.", code: "model.train()\nfor xb, yb in train_loader:\n    xb, yb = xb.to(device), yb.to(device)\n    optimizer.zero_grad()\n    logits = model(xb)\n    loss = criterion(logits, yb)\n    loss.backward()\n    optimizer.step()", gotcha: "The correct order is zero_grad → forward → loss → backward → step. Calling step before backward updates with stale or missing gradients.", question: "Why call `model.train()` before training?", options: ["It enables training behavior for layers such as Dropout and BatchNorm.", "It calculates accuracy.", "It saves a checkpoint.", "It disables gradients."], correct: 0, explanation: "Some layers use different training and evaluation behavior, so mode selection is an explicit part of a correct loop.", next: "Training loss is not enough; evaluate cleanly on data the model did not fit." },
  { slug: "evaluation", title: "Evaluation and Metrics", subtitle: "Measure generalization without accidentally training on validation data.", minutes: 20, tags: ["Evaluation", "Accuracy", "no_grad"], description: "Evaluation answers whether the trained model works on unseen examples. It must disable training-specific behavior and avoid building unnecessary gradient graphs.", diagram: "flowchart LR\n V[Validation batch] --> E[model.eval()]\n E --> N[torch.no_grad()]\n N --> P[Predictions]\n P --> M[Metrics and errors]", math: "Accuracy is correct predictions divided by total predictions. For imbalanced tasks, use precision, recall, F1, or ROC-AUC according to the cost of mistakes.", code: "correct = total = 0\nmodel.eval()\nwith torch.no_grad():\n    for xb, yb in valid_loader:\n        pred = model(xb.to(device)).argmax(dim=1).cpu()\n        correct += (pred == yb).sum().item()\n        total += yb.numel()\nprint(f\"accuracy: {correct / total:.3f}\")", gotcha: "Do not choose hyperparameters repeatedly using the test set. Reserve it for the final estimate after validation choices are complete.", question: "Why use `torch.no_grad()` during evaluation?", options: ["It avoids recording gradients, reducing memory use and computation.", "It improves labels.", "It shuffles validation rows.", "It enables Dropout."], correct: 0, explanation: "Evaluation needs predictions, not parameter derivatives. Disabling graph construction makes it faster and lighter.", next: "Move the full pipeline to a GPU safely and efficiently." },
  { slug: "gpu-training", title: "GPU Training", subtitle: "Use accelerated hardware without device-mismatch and memory surprises.", minutes: 18, tags: ["GPU", "CUDA", "Memory"], description: "GPUs excel at the large parallel matrix operations in deep learning. Correct GPU training is largely about keeping data, model, and metric handling consistent.", diagram: "flowchart LR\n CPU[CPU DataLoader batch] --> T[to(device)]\n M[Model to device] --> F[GPU forward/backward]\n T --> F\n F --> C[Optional .cpu() for metrics]\n C --> CPU", math: "GPU acceleration changes where operations run, not the learning equations. Transfers have a cost, so move whole batches and avoid repeated CPU-GPU copies inside the inner loop.", code: "device = torch.device(\"cuda\" if torch.cuda.is_available() else \"cpu\")\nmodel = model.to(device)\nfor xb, yb in train_loader:\n    xb = xb.to(device, non_blocking=True)\n    yb = yb.to(device, non_blocking=True)\n    # forward, loss, backward, step", gotcha: "A device mismatch error means at least one input, target, model parameter, or auxiliary tensor is on a different device. Print `.device` for each suspect tensor.", question: "Which change most directly prevents a device-mismatch error?", options: ["Move the model and every batch tensor to the same selected device.", "Increase batch size.", "Use more DataLoader workers.", "Call model.eval()."], correct: 0, explanation: "All operands participating in a PyTorch operation must live on compatible devices.", next: "Save the trained work so it can be resumed, tested, and deployed." },
  { slug: "saving-loading-models", title: "Saving and Loading Models", subtitle: "Create portable checkpoints for inference and resumed training.", minutes: 18, tags: ["Checkpoints", "state_dict", "Inference"], description: "A checkpoint preserves learned weights and, when needed, the optimizer state and epoch. It is the bridge between training experiments and usable inference.", diagram: "flowchart LR\n T[Trained model] --> S[state_dict checkpoint]\n S --> D[Disk]\n D --> L[New model instance]\n L --> I[Load state_dict]\n I --> P[Inference or resume]", math: "A state dictionary is a mapping from registered parameter/buffer names to tensors. Recreating the same architecture then loading that mapping restores its learned function.", code: "# Save weights\ntorch.save(model.state_dict(), \"classifier.pt\")\n\n# Load into the same architecture\nloaded = Classifier().to(device)\nloaded.load_state_dict(torch.load(\"classifier.pt\", map_location=device, weights_only=True))\nloaded.eval()", gotcha: "Save `state_dict`, not a pickled whole model object, for durable checkpoints. Whole-object serialization depends on the original Python class path.", question: "What else should a resume-training checkpoint usually contain?", options: ["Optimizer state, epoch, and relevant training metadata.", "Only validation predictions.", "The GPU model name only.", "A copy of every raw image."], correct: 0, explanation: "Optimizers such as Adam have internal moving averages; restoring them helps resumed training continue consistently.", next: "Finally, adapt a pretrained network instead of learning visual features from zero." },
  { slug: "transfer-learning", title: "Transfer Learning", subtitle: "Fine-tune pretrained representations for a new task with limited data.", minutes: 24, tags: ["Transfer learning", "Fine-tuning", "Pretrained models"], description: "Pretrained models already encode broad patterns. Transfer learning reuses them, replacing or tuning only the parts needed for your data and labels.", diagram: "flowchart LR\n P[Pretrained backbone] --> F[General features]\n F --> H[Replace task head]\n H --> T[Train on new labelled data]\n T --> FT[Optionally unfreeze and fine-tune]\n FT --> R[Specialized model]", math: "Freezing a parameter means excluding it from gradient updates. Fine-tuning unfreezes some or all pretrained layers, usually with a smaller learning rate than a newly initialized classification head.", code: "from torchvision.models import resnet18, ResNet18_Weights\nimport torch.nn as nn\n\nmodel = resnet18(weights=ResNet18_Weights.DEFAULT)\nfor p in model.parameters(): p.requires_grad = False\nmodel.fc = nn.Linear(model.fc.in_features, num_classes)\n# First train model.fc; later unfreeze selected backbone layers carefully.", gotcha: "Apply the pretrained model's documented image normalization and input size. Mismatched preprocessing can erase the benefit of its learned representation.", question: "Why train a new classification head first?", options: ["It adapts output labels while protecting useful pretrained features from large early updates.", "It removes the need for labels.", "It guarantees no overfitting.", "It makes the backbone smaller."], correct: 0, explanation: "The new head starts random, so stabilizing it before unfreezing the backbone reduces the risk of damaging pretrained features.", next: "You can now build, train, evaluate, accelerate, save, and adapt PyTorch models end to end." },
];

function sections(s: Spec): Section[] { return [
  { step: 1, title: "What you will learn", blocks: [{ type: "text", content: s.description }] },
  ...(s.install ? [{ step: 2, title: "Install it", blocks: [{ type: "code" as const, language: "bash", label: "Windows PowerShell", code: s.install }, { type: "callout" as const, kind: "tip" as const, content: "Use `python -m pip` rather than bare `pip` so packages install into the Python environment you just activated. For a GPU-specific wheel, always use the current command from the official PyTorch selector." }] }] : []),
  { step: s.install ? 3 : 2, title: "Visual explanation", blocks: [{ type: "diagram", chart: s.diagram, label: `${s.title} in context` }] },
  { step: s.install ? 4 : 3, title: "Key idea", blocks: [{ type: "callout", kind: "math", content: s.math }] },
  { step: s.install ? 5 : 4, title: "Run it", blocks: [{ type: "code", language: "python", label: "Minimal PyTorch example", code: s.code }] },
  { step: s.install ? 6 : 5, title: "Experiment", blocks: [{ type: "text", content: "Run this example, print shapes and devices, then change one setting. Record what changes and why before moving to a larger dataset." }] },
  { step: s.install ? 7 : 6, title: "Common mistake", blocks: [{ type: "callout", kind: "gotcha", content: s.gotcha }] },
  { step: s.install ? 8 : 7, title: "Test yourself", blocks: [{ type: "quiz", question: s.question, options: s.options, correct: s.correct, explanation: s.explanation }] },
  { step: s.install ? 9 : 8, title: "What is next", blocks: [{ type: "text", content: s.next }] },
]; }

export const pytorchLessons: Lesson[] = specs.map((s, index) => ({ slug: s.slug, trackSlug: "pytorch", order: index + 1, minutes: s.minutes, title: s.title, subtitle: s.subtitle, tags: s.tags, sections: sections(s) }));
